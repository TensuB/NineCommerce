@model BlogPostListModel
@{
    Layout = "_ColumnsOne";

    //title
    Html.AddTitleParts(T("PageTitle.Blog").Text);
    //page class
    Html.AppendPageCssClassParts("html-blog-page");
}

<!-- blog -->
<section class="bg-light separator-top section-cover" style="background-image: url('../Themes/MukeraCleanTheme/Content/images/header-backgrounds/Dr-Barry-story-page-header-backgroud-third.png')">
  <div class="container">
    <div class="row justify-content-center">
        <div class="col-12 mt-10 text-center">
            <h1><b>Writing </b></h1>
            <hr />
            <h3>@Model.PagingFilteringContext.Tag</h3>
        </div>
    </div>
  </div>             
</section>

<section class="bg-dark separator-top section-cover">
  <div class="container">
    <div class="row justify-content-center" id="writingHeaderSection">
        <div class="col-lg-12 text-white text-left">
            <h2>@Model.PagingFilteringContext.Tag  Overview</h2>
            <hr/>

            @if(@Model.BlogPostSubCategoriesByCount.Count() > 0)
            {
                <p>
                 The top categroy of @Model.PagingFilteringContext.Tag has the following @Model.BlogPostSubCategoriesByCount.Count().ToString() subcategories:
                </p> 
                <ul>
                    @foreach(var category in Model.BlogPostSubCategoriesByCount)
                    {
                       <li>
                          <h4> <a href="@Url.RouteUrl("BlogPostByTagAndSubCategory", 
                                new { tag = @Model.PagingFilteringContext.Tag,
                                      subCategory = category.Key})">@category.Key (@category.Value)</a> 
                           </h4>
                        </li> 
                    }
                </ul>
            }
            else
            {
                <p>
                 No writing subcategories are found for the current tag!
                </p>
            }

        </div>

    </div>
  </div>  
    
  <!--
  <div class="container">
    <div class="row justify-content-center" id="writingsList">
        <div class="col-lg-12 text-white text-left">         
            <hr/>
        </div>

        <div class="col-sm-12 col-md-6 col-lg-4">
            @foreach(var subcategory in Model.BlogPostSubCategoriesByCount)
            {
                <div class="col-sm-12 col-md-6 col-lg-4">
                    <a href="@Url.RouteUrl("BlogByTagAndSubCategory", new { tag = @Model.PagingFilteringContext.Tag, subCategory = subcategory.Key  })" class="tile-content">
                        <h3>@subcategory.Key (@subcategory.Value.ToString())</h3>
                    </a>                
                </div>
            }
        </div>

    </div>
  </div>          
   -->
</section>
<!-- / blog -->

